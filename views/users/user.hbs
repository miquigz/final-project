<h2 class="h1 mb-5 text-center text-success font-weight-bold">Informacion del Usuario</h2>
<div class="mt-2 bg-sec-opacity py-3 rounded border border-light pt-5 pb-1 mx-5 rounded">
    {{#if editar}}<div class="d-flex justify-content-end"><a href="/user/edit/{{user.slugUser}}" class="btn btn-light mx-2">Editar Perfil</a></div>{{/if}}
    <h3 class="text-center text-success font-weight-bold">
        {{ user.name }}
    </h3>
    <div class="d-flex justify-content-center">
        {{#if user.desc}}
            {{{user.desc}}}
        {{/if}}
    </div>
    <div>
    </div>
        <p class="text-muted mx-2">Fecha de creacion: {{user.createdAt}}</p>
        <p class="text-muted mx-2">Ultima vez editado: : {{user.updatedAt}}</p>
    {{#if post.user}}
        <p class="text-muted mx-2">Creado por: <label class="text-success">{{post.user}}</label></p> 
    {{/if}}
</div>